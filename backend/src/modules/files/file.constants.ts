export const FILE_LIMITS = {
  MAX_SIZE_BYTES: 100 * 1024 * 1024,
  MAX_FILENAME_LENGTH: 255,
  MAX_EXTENSION_LENGTH: 20,
  // Values are SECONDS (the S3 presigner contract), not milliseconds.
  UPLOAD_URL_EXPIRATION_SECONDS: 60 * 60,
  DOWNLOAD_URL_EXPIRATION_SECONDS: 5 * 60,
  DEFAULT_LIST_LIMIT: 20,
  MAX_LIST_LIMIT: 50,
  MULTIPART_MAX_SIZE_BYTES: 5 * 1024 * 1024 * 1024,
  MULTIPART_PART_SIZE_BYTES: 8 * 1024 * 1024,
  MULTIPART_MAX_PART_COUNT: 10000,
  MULTIPART_STALE_MS: 24 * 60 * 60 * 1000,
} as const;

export const FILE_SIZES = {
  KILOBYTE: 1024,
  MEGABYTE: 1024 * 1024,
} as const;
